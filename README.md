# Git 연습 중 입니다.
나의 첫번째 웹페이지
- MARK DOWN 문법
- `init` 은 초기화 할 때 (숫자 1번 옆에 있는 따옴표)
- `add` stage 영역에 올릴 때
- `commit` 은 스냅샷 찍을 때
- push
- `#`h태그를 말한다.

## css도 배울 수 있당
```css
  h1{
    color : red ;
    }
```
